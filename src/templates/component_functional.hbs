import React from 'react';{{> styleImport }}
{{> withClassNameImport }}

{{ withClassNameProps }} {
}

const {{pascalCase name }} = (props: Props): React.ReactElement => {
  return (
    <{{ templateBaseComponent }} {{> withClassNameClassName }}data-testid={"{{kebabCase name }}-root"}>
      {{ name }}
    </{{ templateBaseComponent }}>
  );
}

export { {{pascalCase name }} }
